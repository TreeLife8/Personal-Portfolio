var popover = new bootstrap.Popover(
  document.querySelector(".example-popover-1"),
  {
    container: "body",
  }
);
var popover = new bootstrap.Popover(
  document.querySelector(".example-popover-2"),
  {
    container: "body",
  }
);
var popover = new bootstrap.Popover(
  document.querySelector(".example-popover-3"),
  {
    container: "body",
  }
);
var popover = new bootstrap.Popover(
  document.querySelector(".example-popover-4"),
  {
    container: "body",
  }
);
var popover = new bootstrap.Popover(
  document.querySelector(".example-popover-5"),
  {
    container: "body",
  }
);
var popover = new bootstrap.Popover(
  document.querySelector(".example-popover-6"),
  {
    container: "body",
  }
);
var popover = new bootstrap.Popover(
  document.querySelector(".example-popover-7"),
  {
    container: "body",
  }
);
var popover = new bootstrap.Popover(
  document.querySelector(".example-popover-8"),
  {
    container: "body",
  }
);
